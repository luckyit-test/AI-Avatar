# ✅ Проверка успешности деплоя

## 🎯 Быстрая проверка:

### 1. Проверьте сайт в браузере:
Откройте: **https://newava.pro/**

Если сайт открывается и работает - деплой успешен! ✅

### 2. Проверьте через скрипт:

Запустите файл **`check-deploy.bat`** (если установлен PuTTY)
или **`verify-deploy.ps1`** в PowerShell

### 3. Ручная проверка через SSH:

1. Откройте PowerShell
2. Выполните:
   ```bash
   ssh root@43.245.226.24
   ```
   Пароль: `Yd2Vc_Wejus0DlNB`

3. На сервере выполните:
   ```bash
   cd /opt/newava
   
   # Проверка ветки
   git branch --show-current
   
   # Проверка последнего коммита
   git log -1 --oneline
   
   # Проверка контейнеров
   docker compose ps
   
   # Проверка логов
   docker compose logs --tail 20
   ```

## ✅ Признаки успешного деплоя:

1. ✅ Сайт https://newava.pro/ открывается
2. ✅ Ветка на сервере: `avatar`
3. ✅ Контейнеры запущены: `docker compose ps` показывает статус "Up"
4. ✅ Последний коммит соответствует локальному

## ❌ Если что-то не так:

### Сайт не открывается:
```bash
ssh root@43.245.226.24
cd /opt/newava
docker compose ps
docker compose logs --tail 50
```

### Ветка не avatar:
```bash
ssh root@43.245.226.24
cd /opt/newava
git fetch origin
git checkout avatar
git pull origin avatar
docker compose up -d --build
```

### Контейнеры не запущены:
```bash
ssh root@43.245.226.24
cd /opt/newava
docker compose up -d --build
docker compose ps
```

## 📊 Ожидаемый результат:

После успешного деплоя вы должны увидеть:

```
Ветка: avatar
Коммит: [последний коммит из ветки avatar]
Контейнеры: все в статусе "Up"
Сайт: https://newava.pro/ работает
```

