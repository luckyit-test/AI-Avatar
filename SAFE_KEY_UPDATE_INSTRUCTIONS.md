# 🔒 Безопасное обновление API ключей

## ✅ Защита от попадания ключей в git

### Что уже настроено:

1. ✅ `.env` файлы в `.gitignore` - ключи на сервере не попадут в git
2. ✅ Старые скрипты с ключами заблокированы
3. ✅ Создан безопасный скрипт `update-api-keys-safe.bat`

## 🚀 Как обновить ключи безопасно

### Вариант 1: Использовать безопасный скрипт (рекомендуется)

1. Запустите `update-api-keys-safe.bat`
2. Скрипт запросит ключи интерактивно
3. Ключи **НЕ сохраняются** в файл, передаются напрямую на сервер
4. После использования переменные очищаются из памяти

### Вариант 2: Обновить через SSH вручную

```bash
# Подключитесь к серверу
ssh root@43.245.226.24
# Пароль: Yd2Vc_Wejus0DlNB

# Обновите ключи для hnyear.com
cd /opt/hnyear
nano .env
# Замените GEMINI_API_KEY и GEMINI_API_KEY_ANALYSIS на новые ключи
# Сохраните: Ctrl+O, Enter, Ctrl+X

# Обновите ключи для newava.pro
cd /opt/newava
nano .env
# Замените GEMINI_API_KEY и GEMINI_API_KEY_ANALYSIS на новые ключи
# Сохраните: Ctrl+O, Enter, Ctrl+X

# Перезапустите контейнеры
cd /opt/hnyear && docker compose restart backend
cd /opt/newava && docker compose restart backend
```

## ⚠️ ВАЖНО: Правила безопасности

1. ❌ **НИКОГДА** не коммитьте файлы с ключами
2. ❌ **НИКОГДА** не добавляйте ключи в скрипты, которые попадут в git
3. ✅ Используйте `.env` файлы (они в `.gitignore`)
4. ✅ Используйте интерактивные скрипты, которые запрашивают ключи
5. ✅ Проверяйте файлы перед коммитом: `git diff` и `git status`

## 🔍 Проверка перед коммитом

Перед каждым коммитом проверяйте:

```bash
# Проверьте, нет ли ключей в измененных файлах
git diff | grep -i "AIzaSy\|API_KEY\|SECRET"

# Проверьте статус - не попали ли .env файлы
git status | grep -i "\.env"

# Если что-то найдено - НЕ КОММИТЬТЕ!
```

## 📋 Чеклист безопасности

- [ ] Ключи НЕ в файлах, которые будут закоммичены
- [ ] `.env` файлы в `.gitignore`
- [ ] Использован безопасный скрипт или SSH
- [ ] Проверен `git diff` перед коммитом
- [ ] Ключи обновлены на сервере
- [ ] Контейнеры перезапущены

## 🛡️ Дополнительная защита

Если случайно добавили ключи в файл:

```bash
# Отмените добавление файла
git reset HEAD <файл>

# Удалите ключи из файла
# Отредактируйте файл и удалите ключи

# Проверьте еще раз
git diff | grep -i "AIzaSy"
```

