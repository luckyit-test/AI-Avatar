# Контекст проекта AI Avatar

## Текущее состояние проекта

**Ветка**: `avatar`  
**Домен продакшена**: `newava.pro`  
**Последний коммит**: `8cf94a3` - Fix gallery query: remove imagesCount > 0 condition and fix imagesCount calculation

## Архитектура

### Frontend
- **Фреймворк**: React + TypeScript + Vite
- **Стилизация**: Tailwind CSS
- **Анимации**: Framer Motion
- **Основные компоненты**:
  - `App.tsx` - главный компонент приложения
  - `components/ImageUploadFlow.tsx` - загрузка изображений (Шаг 1)
  - `components/ImageConfiguration.tsx` - настройка параметров (Шаг 2)
  - `components/GenerationActions.tsx` - генерация портретов (Шаг 3)
  - `components/Footer.tsx` - подвал сайта
  - `components/GalleryPage.tsx` - страница галереи
  - `components/AnimatedPortraitsBackground.tsx` - анимированный фон с портретами

### Backend
- **Сервер**: Node.js + Express
- **API**: REST API с префиксом `/api`
- **Генерация изображений**: Google Gemini API (`gemini-2.5-flash-image`)
- **Обработка изображений**: Sharp (resize, format conversion, optimization)
- **База данных**: SQLite (заказы, промокоды)
- **Очередь генерации**: In-memory очередь с rate limiting

### Ключевые файлы сервера
- `server/index.js` - основной сервер, API endpoints
- `server/bot/telegram.js` - Telegram бот для генерации портретов
- `server/services/promptBuilder.js` - построение промптов для генерации
- `server/services/promptUtils.js` - утилиты для промптов (одежда, роли, компании)
- `server/services/portraitGeneration.js` - логика генерации портретов
- `server/services/imageProcessing.js` - предобработка изображений (denoise, background replacement)
- `server/db/orders.js` - работа с заказами
- `server/db/promocodes.js` - работа с промокодами

## Важные настройки

### Промпт для генерации
- **Модель**: `gemini-2.5-flash-image`
- **Стили**: 6 стилей (Классический, Современный, Креативный, Технологичный, Дружелюбный, Уверенный)
- **Особенности промпта**:
  - Максимальное сглаживание кожи (glossy magazine cover style)
  - Удаление пор, зернистости, шума
  - Сохранение закрытого рта (если закрыт в оригинале)
  - Легкое уменьшение полноты лица для полных людей
  - Запрет на показ оборудования для фотосъемки в кадре
  - Естественная глубина резкости фона (не слишком размыто)
  - Минимум 3 разных варианта одежды в подборке из 6 портретов

### Предобработка изображений
- **Удаление шума**: Медианный фильтр + легкое размытие + повышение резкости
- **Замена фона**: Серый градиент (RGB 128, 128, 128)
- **Размер**: 1024x1024 с сохранением пропорций

### Оптимизация изображений
- **Формат**: WebP с fallback на JPEG
- **Размеры**: Small (150px) для мобильных, Medium (200px) для десктопа
- **Качество**: JPEG quality 85 для оптимизированных версий
- **Lazy loading**: Intersection Observer для фоновых портретов
- **Сохранение**: JPEG quality 95, максимальный размер 2048px

### Оплата
- **Провайдер**: Robokassa
- **Стоимость генерации**: 200 ₽
- **Промокоды**: Поддерживаются через `/api/promo/use`

## Деплой

### Скрипты деплоя
- `deploy-avatar-to-prod.bat` - деплой ветки `avatar` на `newava.pro`
- `deploy-new-year-to-prod.bat` - деплой ветки `new-year` на `hnyear.com`

### Docker Compose
- **Сервисы**:
  - `newava_app` - фронтенд (Nginx)
  - `newava_backend` - бэкенд (Node.js)
  - `newava_nginx` - реверс-прокси
  - `newava_certbot` - SSL сертификаты

### Nginx конфигурация
- `deploy/nginx/conf.d/app.conf` - конфигурация для `newava.pro`
- `deploy/nginx/conf.d/hnyear.conf` - конфигурация для `hnyear.com`

## Важные моменты

### Два проекта на одном сервере
- **newava.pro** (ветка `avatar`) - основной проект
- **hnyear.com** (ветка `new-year`) - второй проект
- Оба используют один Nginx контейнер, но разные конфигурации
- При деплое одного проекта нужно сохранять конфигурацию другого

### Email поддержки
- **Текущий email**: `info@i-integrator.com`
- Используется в кнопках поддержки (открывают Gmail compose)
- Используется в правилах генераций

### Правила генераций
- Добавлен пункт о правах на изображения
- При генерации пользователь дает право использовать портреты в качестве примеров

## API Endpoints

### Генерация
- `POST /api/generate-image` - генерация одного портрета
- `GET /api/generate-image/:jobId` - статус генерации
- `POST /api/promo/use` - генерация с промокодом (6 портретов)

### Валидация и анализ
- `POST /api/validate-image` - валидация изображения
- `POST /api/detect-gender` - определение пола
- `POST /api/evaluate-image` - полный анализ изображения

### Галерея
- `GET /api/gallery/recent` - последние портреты для фона (исключает Telegram бот)
- `GET /api/gallery/orders` - заказы для галереи (исключает Telegram бот)
- `GET /api/diagnostics/gallery` - диагностика галереи (без пароля, показывает статистику заказов)

### Диагностика
- `GET /api/diagnostics/gallery` - диагностический эндпоинт для проверки состояния галереи
  - Показывает последние заказы, статистику по paymentType, статус кэша
  - Доступен без авторизации (только чтение диагностических данных)

### Оптимизация изображений
- `GET /api/images/optimized?url=...&size=...&format=...` - оптимизированные версии

## Роли и компании

### Роли (12 вариантов)
1. Разработчик
2. Тимлид
3. Архитектор
4. DevOps-инженер
5. Дата-сайентист
6. ML-инженер
7. Продуктовый менеджер
8. Проектный менеджер
9. Системный аналитик
10. Дизайнер UI/UX
11. QA-инженер
12. CTO

### Типы компаний (17 вариантов)
1. Стартап
2. Продуктовая компания
3. Enterprise
4. Аутсорс/консалтинг
5. Госкомпания
6. Финтех
7. Банк
8. Страховая
9. Ритейл
10. Маркетплейс
11. Медиа
12. EdTech
13. HealthTech
14. Телеком
15. Производство
16. Логистика
17. GameDev

## Одежда

### Логика выбора одежды
- Зависит от пола, роли и типа компании
- Формальные компании: Business Casual/Business Formal
- Современные компании: Smart Casual
- Гарантируется минимум 3 разных варианта одежды в подборке из 6 портретов
- Подробная раскладка в `CLOTHING_MATRIX.md`

## Галерея портретов

### Фильтрация заказов
- **Включены**: Заказы с `paymentType = NULL` (веб-заказы) или `paymentType = 'promo'` (промокоды)
- **Исключены**: Заказы с `paymentType LIKE 'telegram_%'` (Telegram бот)
- **Условия**: `status = 'completed'`, `generatedImagesJson IS NOT NULL`, `LENGTH(generatedImagesJson) > 10`

### Кэширование
- **Статус**: Кэш отключен для надежности (всегда загружаются свежие данные из БД)
- **Очистка**: Кэш очищается автоматически после генерации новых портретов
- **Функция**: `clearGalleryCache()` вызывается из `generatePortraitsForOrder` после сохранения

### Проблемы и исправления
- **Проблема**: Новые портреты не отображались из-за условия `imagesCount > 0` в SQL запросе
- **Причина**: `imagesCount` мог быть равен 0, хотя `generatedImagesJson` содержал данные
- **Решение**: Убрано условие `imagesCount > 0`, добавлена проверка длины JSON (`LENGTH(generatedImagesJson) > 10`)
- **Дополнительно**: Исправлено вычисление `imagesCount` в `saveOrder` - теперь пересчитывается из `generatedImages`

## Полезные команды

### Git
```bash
# Проверка текущей ветки
git branch --show-current

# Просмотр последних коммитов
git log --oneline -10

# Переключение на ветку avatar
git checkout avatar

# Обновление из удаленного репозитория
git pull origin avatar
```

### Деплой
```bash
# Деплой на прод
.\deploy-avatar-to-prod.bat
```

### Диагностика
```bash
# Проверка логов через API (без пароля)
# Откройте в браузере: https://newava.pro/api/diagnostics/gallery

# Или используйте bat файлы:
.\check-gallery-via-api.bat      # Открывает диагностику в браузере
.\CHECK-LOGS.bat                 # Показывает команды для ручной проверки
.\check-gallery-logs.bat         # Проверка через SSH (требует SSH ключ)
```

## Telegram Bot

### Статус
- ✅ **Реализован и запущен** на продакшене
- **Файл**: `server/bot/telegram.js`
- **Токен**: Хранится в переменной окружения `TELEGRAM_BOT_TOKEN` на сервере

### Функциональность

#### 1. Бесплатная генерация (1 раз навсегда)
- 1 портрет, один раз на пользователя
- Фиксированные параметры: роль "Разработчик", компания "Стартап"
- Сохраняется в БД таблице `telegram_free_generations`
- `paymentType`: `telegram_free`

#### 2. Платная генерация (6 портретов за 200₽)
- Пользователь выбирает роль и компанию через inline клавиатуру
- Оплата через Robokassa или Telegram Stars
- Генерация 6 портретов в разных стилях
- `paymentType`: `telegram_robokassa` или `telegram_stars`

#### 3. Премиум генерация (1 портрет за 500₽ / 300 Stars)
- ⏳ В разработке (планируется)

### Особенности
- Использует прямую генерацию через Gemini API (не через очередь)
- Не мешает работе основного веб-приложения
- Использует существующие сервисы: `promptBuilder`, `imageProcessing`, `validation`
- Портреты из Telegram бота **НЕ попадают** в галерею сайта (фильтруются по `paymentType`)
- Сохранение портретов: файлы сохраняются на диск через `saveImageForOrder`
- Обработка callback_data: используется сокращенный file_id (40 символов) из-за ограничения Telegram (64 байта)

### Команды
- `/start` - приветствие и описание функционала
- `/help` - справка

### Документация
- `TELEGRAM_BOT_SETUP.md` - подробная инструкция по настройке
- `TELEGRAM_BOT_QUICK_START.md` - быстрый старт

## База данных

### Таблицы
- **orders**: Заказы с полями `invId`, `status`, `paymentType`, `promoCode`, `generatedImagesJson`, `imagesCount`
- **promocodes**: Промокоды с лимитами использования
- **telegram_free_generations**: Статус бесплатной генерации для пользователей Telegram бота

### Важные поля orders
- `paymentType`: `NULL` (веб), `'promo'` (промокод), `'telegram_free'`, `'telegram_robokassa'`, `'telegram_stars'`
- `imagesCount`: Количество портретов (пересчитывается из `generatedImages` при сохранении)
- `generatedImagesJson`: JSON строка с путями к портретам (формат: `{"Стиль": "/images/orders/invId/file.jpg"}`)

## Следующие шаги / TODO

- [x] Добавить возможность выбора роли и компании через команды бота (реализовано)
- [x] Добавить возможность оплаты через бота (Robokassa и Telegram Stars) (реализовано)
- [ ] Реализовать премиум генерацию (1 портрет за 500₽ / 300 Stars)
- [ ] Добавить сохранение истории генераций для пользователя Telegram бота
- [ ] Возможные улучшения промпта по результатам тестирования
- [ ] Оптимизация производительности генерации
- [ ] Добавить админ-панель для управления Telegram ботом

## Контакты и поддержка

- **Email**: info@i-integrator.com
- **Телефон**: +7 925 685-25-25
- **Домен**: https://newava.pro/

## Примечания

- Все изменения промпта делаются в `server/services/promptBuilder.js`
- Логика одежды находится в `server/services/promptUtils.js`
- При изменении промпта всегда тестировать на реальных изображениях
- При деплое ветки `avatar` сохраняется конфигурация `hnyear.com`
- При деплое ветки `new-year` сохраняется конфигурация `newava.pro`
- Портреты из Telegram бота автоматически исключаются из галереи сайта
- Галерея всегда загружает свежие данные из БД (кэш отключен для надежности)
- При сохранении заказа `imagesCount` пересчитывается из `generatedImages`, если равен 0
- Диагностический эндпоинт `/api/diagnostics/gallery` доступен без пароля для проверки состояния галереи

