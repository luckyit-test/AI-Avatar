# 🚨 СРОЧНЫЙ ДЕПЛОЙ: Переключение на ветку avatar

## ❌ ПРОБЛЕМА ОБНАРУЖЕНА:

**На проде сейчас:**
- Ветка: `main` ❌
- Коммит: `9270e60` (старый)

**Нужно:**
- Ветка: `avatar` ✅
- Коммит: `16dd6e1` или новее ✅

## ✅ РЕШЕНИЕ:

### Способ 1: Автоматический деплой (рекомендуется)

1. **Убедитесь, что PuTTY установлен**
   - Если нет: https://www.putty.org/

2. **Запустите файл: `deploy-avatar-to-prod.bat`**
   - Он автоматически:
     - Переключит ветку на `avatar`
     - Обновит код
     - Пересоберет контейнеры
     - Перезапустит их

3. **Проверьте результат:**
   - Запустите `check-prod-branch.bat`
   - Должно показать: ветка `avatar`

### Способ 2: Ручной деплой через PuTTY

Если `deploy-avatar-to-prod.bat` не работает, выполните вручную:

```bash
plink.exe -ssh root@43.245.226.24 -pw Yd2Vc_Wejus0DlNB
```

Затем на сервере:
```bash
cd /opt/newava
git fetch origin
git checkout avatar
git pull origin avatar
docker compose down
docker compose up -d --build
docker compose ps
```

## 📊 Что будет сделано:

1. ✅ Переключение с ветки `main` на `avatar`
2. ✅ Обновление кода до последней версии из `avatar`
3. ✅ Пересборка Docker контейнеров
4. ✅ Перезапуск контейнеров с новым кодом

## 🔍 Проверка после деплоя:

1. Запустите `check-prod-branch.bat`
2. Должно показать:
   - Ветка: `avatar` ✅
   - Коммит: `16dd6e1` или новее ✅
3. Проверьте сайт: https://newava.pro/

## ⚠️ ВАЖНО:

После деплоя контейнеры будут перезапущены, что может вызвать кратковременную недоступность сайта (несколько секунд).

